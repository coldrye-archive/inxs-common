{
  "name": "inxs-common",
  "version": "0.0.1",
  "description": "Collection of abstract base classes for implementing custom injection frameworks for Babel (node, browser).",
  "license": "Apache-2.0",
  "homepage": "http://es.coldrye.eu/inxs-common/",
  "main": "dist/inxs-common.js",
  "bugs": {
    "url": "https://github.com/coldrye-es/inxs-common/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/coldrye-es/inxs-common.git"
  },
  "author": {
    "name": "Carsten Klein",
    "email": "silkentrance@gmail.com"
  },
  "maintainers": [
    {
      "name": "cklein",
      "email": "silkentrance@gmail.com"
    }
  ],
  "scripts": {
    "clean": "test -f ./package.json && test -d ./dist -o -d ./build -o ./coverage && rm -Rf ./dist ./build ./coverage || true",
    "build": "test ! -d ./build && BABEL_ENV='BUILD' babel -d build test",
    "cover": "test -d ./build && babel-istanbul cover _mocha --report html build/*-test.js && konqueror ./coverage/index.html",
    "dist": "test ! -d ./dist && BABEL_ENV='DIST' babel -d dist src",
    "test": "BABEL_ENV='TEST' mocha --compilers js:babel/register ./test/*"
  },
  "dependencies": {
    "sprintf-js": "^1.0.3"
  },
  "devDependencies": {
    "babel": "^5.8.21",
    "babel-eslint": "^4.1.1",
    "babel-istanbul": "^0.3.20",
    "eslint": "^1.3.1",
    "eslint-config-coldrye": "coldrye-es/eslint-config-coldrye#v0.0.4",
    "mocha": "^2.2.5"
  },
  "files": [
    "dist"
  ],
  "babel": {
    "externalHelpers": true,
    "optional": [
      "runtime"
    ],
    "env": {
      "TEST": {
        "auxiliaryCommentBefore": "istanbul ignore next"
      },
      "BUILD": {
        "compact": false
      },
      "DIST": {
        "compact": true,
        "comments": false
      }
    }
  }
}
